This project is here to make the fallout terminal hacking trivial.

To streamline testing with fallout you can now use autospec to always run your tests.

just run autospec from the base directory of the project.

